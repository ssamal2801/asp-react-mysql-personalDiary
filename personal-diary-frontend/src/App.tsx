import { styled } from 'styled-components';
import './App.css'

function App() {

  const AppContainer = styled.div`
    min-height: 100vh;`;

  return (
    <AppContainer>
      this is the app
    </AppContainer>
  )
}

export default App
